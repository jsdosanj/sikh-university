---
import { topicName } from '../lib/data';
import type { Course } from '../lib/data';
interface Props { course: Course; }
const { course: c } = Astro.props;
---
<a href={`/course/${c.id}`} class="card group">
  <div class="mb-1.5 flex flex-wrap items-center gap-2 font-sans text-xs text-muted">
    <span class="pill bg-[#e7ecf5] text-navy">{topicName(c.topic)}</span>
    <span class="pill bg-paper text-muted">{c.level} level</span>
    {c.aiCreated && <span class="pill bg-saffron-soft text-saffron-deep">Created by AI</span>}
  </div>
  <h3 class="text-lg font-bold leading-snug text-navy group-hover:text-saffron-deep">{c.title}</h3>
  <div class="mt-0.5 font-sans text-xs text-muted">Prof. {c.professor}</div>
  <p class="mt-2 text-[0.95rem] text-ink/90">{c.summary}</p>
</a>
